// Copy this file to the same folder with names admin1.conf and admin2.conf and the credentials
// of your 2 harvest test accounts
// configuration for unittest, needs an admin access token
// <email-address for the account>
harvest: {
  auth: {
    accountId: ""
    token: ""
  }
  // we could have 2 test run at the same time
  max_request_per_window: 48
}
